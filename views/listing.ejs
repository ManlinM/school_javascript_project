<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <% if(listing==null){ %>

            No Records Found

            <% }else{ %>
                <%= listing.name %>
                    <% } %>
    </title>

    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    </head>
</head>

<body class="ms-5 mt-2">

    <form action="/">
        <button type="submit">Home</button>
        <div class="border p-5 mt-5 bg-light col-6">
            <% if(listing==null ){ %>

                <h4>No Records Found</h4>

                <% }else{ %>

                    <h3>
                        <%= listing.name %> hosted by <a href="mailto:<%=listing.email%>">
                                <%= listing.first_name %>
                                    <%= listing.last_name %>
                            </a>
                    </h3>
                    <p>
                        <%= listing.cityName %>, <%= listing.stateName %>
                    </p>
                    <p>
                        <%=listing.max_guest %> guests, <%=listing.number_bathrooms %> baths
                                @ $<%=listing.price_by_night %> CAD /night
                    </p>
                    <hr />
                    <p>
                        <%- listing.description %>
                    </p>
                    <hr />
                    <b>Amenities Included:</b>

                    <p>
                        <% let stringOne="" ; %>
                            <%for (let i=0; i < amenities.length;i++){%>
                                <% stringOne+=amenities[i].name+", " %>
                                <% } %>
                                <%= stringOne.slice(0, -2) %> 
                    </p>


        </div>
        <% } %>
    </form>
</body>

</html>